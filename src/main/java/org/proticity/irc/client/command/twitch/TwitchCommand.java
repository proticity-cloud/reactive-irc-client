package org.proticity.irc.client.command.twitch;

import reactor.util.annotation.NonNull;

/**
 * A maker interface for server commands which are used in Twitch's TMI extensions.
 *
 * These commands are issued by Twitch's TMI servers to chat clients who negotiate Twitch's
 * vendor-specific capabilities.
 */
public interface TwitchCommand {
}
